# Worker Service Environment Variables

# MongoDB Connection (Required)
# Get this from MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/comparex?retryWrites=true&w=majority

# Worker Authentication Secret (Required)
# Generate a random string - must match WORKER_SECRET in Vercel
WORKER_SECRET=change-this-to-random-secure-string

# Server Port (Optional - Render provides this automatically)
PORT=3001
